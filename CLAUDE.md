# DSS NAS 이식

사내 시스템 넷을 개발 PC에서 **Synology DS218+ NAS의 Docker**로 옮기는 일을 담는 저장소다.

여기에는 **네 시스템을 잇는 것만** 둔다. 어느 한 시스템의 것은 그 저장소에 있어야 한다.
그 경계를 흐리면 포털이 A/S의 배포를 알게 되고, 그때부터 한 시스템을 고칠 때마다
다른 시스템을 함께 봐야 한다.

**이어서 작업할 때는 이 파일과 [README.md](./README.md)를 먼저 읽는다.**
지금 어느 단계인지는 README에 있다.

바탕화면 **「작업 시작」 → 6번 (NAS 이식)** 이 이 저장소에서 Claude를 띄운다.
나머지 저장소 넷은 `--add-dir`로 함께 붙으므로 한 창에서 다섯 곳을 다 고칠 수 있다.
서버는 켜지 않는다 — 필요하면 메뉴 0번을 따로 실행한다.

세션은 `/나스이식시작`으로 열고 `/나스이식종료`로 닫는다. 열 때는 다섯 저장소
상태부터 확인하고, 닫을 때는 README의 단계 표를 갱신한다 — 다음 세션이
그 표 하나로 이어받는다.

---

## 다루는 저장소 넷 — 매번 절대경로를 쓴다

세션은 `C:\Users\희만\Desktop\Development`에서 연다. 네 저장소가 모두 하위에 있어야
한 세션에서 함께 고칠 수 있기 때문이다. 저장소 하나에서 열면 나머지 셋에 손이 닿지 않는다.

| 시스템 | 경로 | 앱 | 개발 DB | NAS 대상 |
|---|---|---|---|---|
| 통합 로그인 | `...\Development\dss-auth` | 3100 | `dss-pg-auth` 5444 | ✅ **운영 중** (09-14) |
| A/S 관리 | `...\Development\RF_Service_System` | 3000 | `dss-pg-app` 5442 (`dss_as` · `dss_as_test`) | ✅ **운영 중** (09-14) |
| 계측기 관리 | `...\Development\njlee` | 3300 | `dss-pg-app` 5442 (`dss_meters`) | ✅ **운영 중** (09-14) |
| 회사 홈페이지 | `...\Development\dss-home` | 3200 | 5436 | ❌ **앱도 DB도 바깥** |

`...` 은 `C:\Users\희만\Desktop`이다. 실행할 때는 줄이지 말고 전부 적는다.

---

## 무엇이 어디에 있나

| 여기 (dss-deploy) | 각 앱 저장소 |
|---|---|
| `nas/docker-compose.nas.yml` — 오케스트레이션 | `Dockerfile` — 그 앱의 이미지 |
| `nas/init/*.sql` — 롤·DB·권한 | `drizzle/` — 그 앱의 마이그레이션 |
| `runbook/` — 이관 절차와 판단 근거 | `.env.local` — 그 앱의 설정 |
| 리버스 프록시·방화벽 설정 | 앱 코드 전부 |

**Dockerfile은 각 저장소에 둔다.** 그 저장소를 빌드하는 파일이기 때문이다.
여기로 모으면 앱을 고친 사람과 이미지를 고칠 사람이 갈라진다.

---

## 확정된 결정 (되돌리려면 근거부터 다시 본다)

| | 결정 | 근거 |
|---|---|---|
| A | **회사 홈페이지는 앱도 DB도 NAS에 두지 않는다** | 공개 사이트를 사내망에 두면 그것 하나 때문에 방화벽에 구멍을 내고, 그 구멍이 나머지 셋 옆에 뚫린다 |
| B | **staging을 NAS에 두지 않는다.** 배포 리허설로 대신한다 | 같은 상자 안의 staging은 운영과 자원을 다퉈 오히려 운영을 해친다 |
| C | **"VPN으로 들어온 사람도 사내에 있는 것으로 본다"** — 계측기 포함 셋 다 밖에서 쓴다 | VPN 사용자도 **사내 주소를 받는다.** 그래서 포털에 등록할 주소가 늘지 않고 앱 설정을 하나도 안 바꾼다. `SECURITY_POLICY.md`와 `njlee/REQUIREMENTS.md:199`가 이 정의로 하나가 된다 |
| D | **A/S는 업무 자료를 이관하지 않는다. 뼈대 20장만 가져간다** (2026-09-04) | 지금 등록된 수리품 258건과 거기 매달린 견적·보고서·고객사·제품모델·재고가 **전부 데모다**(`scripts/seed-dev-db.ts`가 만든 것). 실제 업무는 NAS에서 1번부터 시작한다. **계측기와 통합 로그인은 반대다 — 실제 자료라 그대로 옮긴다** |
| E | **배포는 무중단으로 하지 않는다. 저녁에 10분 멈춘다** (2026-09-07) | 무중단은 앱 컨테이너를 둘 띄워 6GB 중 4.5GB를 쓰고, 전환할 때마다 DSM 프록시를 사람이 두 번 고쳐야 한다. 그러고 얻는 것은 저녁 10분뿐이다 — 개발자 한 명, 배포 주 1회 미만, 사내 15명에서는 남는 장사가 아니다. 근거는 [runbook/05-배포-리허설.md](./runbook/05-배포-리허설.md) 8절 |
| F | **사내 주소는 NAS 고정 IP + 지금과 같은 포트** — `http://192.168.0.222:3100·3000·3300`. 앱 포트는 NAS 자신(`127.0.0.1:13xxx`)에만 열고, 사내망 입구는 DSM 리버스 프록시 하나 (2026-09-14) | 사내 DNS 가 없고 NAS 주소는 DSM 에서 수동 고정이다. DSM 프록시는 NAS 위의 nginx 라 도커 망에 못 들어와 서비스 이름을 모른다 — 그래서 127.0.0.1 에 연다. 사내망 주소에는 여전히 앱 포트가 없다. 도메인은 HTTPS 때(I) |
| G | **계측기 NAS 자료는 개발 PC 사본(08-28 14:23 백업과 같은 자료)** (2026-09-14, 사용자) | 사용자 결정. 이남준 님 PC 의 자동 백업은 08-28 아침부터 전부 0바이트라 그 뒤 변경분을 담은 백업이 없었고, 수동 백업을 받지 않기로 했다. 그 PC 의 계측기는 끄기로 했다 |
| H | **NAS 에서 root 가 필요한 일(docker · 권한)은 사람이 `sudo` 로 실행한다.** Claude 는 `swhur` 열쇠로 스크립트·파일을 올리고 로그로 검수한다 (2026-09-14, 사용자) | "운영 배포는 사람이 한다". docker 권한은 사실상 NAS root 라, 비밀번호 없는 sudo 를 주면 이 PC 의 열쇠 파일 하나가 NAS 전체 열쇠가 된다 |
| I | **HTTPS 는 새 사내용 도메인 + Cloudflare DNS + acme.sh(DNS-01, `synology_dsm` 훅)** — 2026-09-15 착수 (사용자). 도메인 이름은 그때 정한다 | `dss21.com` 의 DNS(카페24)는 acme.sh 가 지원하지 않아 90일 자동 갱신이 안 된다. DDNS + DSM 내장 인증서는 NAS 를 인터넷에 열어야 해 C 에 어긋난다. 근거·비용: 바탕화면 `HTTPS_도입_제안.pptx` |
| — | PostgreSQL **17로 통일** | 17 덤프를 16이 열지 못한다. 되돌려 보지 못하는 백업은 백업이 아니다 |
| — | 인스턴스 **둘** — 인증용 하나, 업무용 하나 | 인증 DB는 등급이 다르다. A/S와 계측기는 등급이 같아 나눌 이유가 없다 |

자세한 근거는 [runbook/01-postgres-통합.md](./runbook/01-postgres-통합.md)에 있다.
결정 C는 [runbook/03-원격-접속.md](./runbook/03-원격-접속.md) 2절에 있다 —
**VPN 방식 자체는 아직 안 정했다(6단계로 보류).** 정한 것은 "사내"의 정의뿐이다.

---

## 이 저장소에서 지켜야 할 것

- **NAS 운영 compose에 DB `ports:`를 쓰지 않는다.** 개발용 파일을 복사해 오면
  `127.0.0.1:54xx:5432`가 딸려 온다. 같은 Docker 네트워크에서 서비스 이름으로 부르면
  DB는 호스트 포트를 하나도 열지 않는다. 이것이 "사내에서 NAS를 통해서만"을 지탱한다.
- **앱 포트도 호스트에 노출하지 않는다.** 리버스 프록시가 유일한 입구여야 한다.
  우회로가 열려 있으면 `TRUSTED_PROXY_HOPS=1`이 보호가 아니라 구멍이 된다
  (`dss-auth/.env.example:55`에 그 이유가 적혀 있다).
- **운영 배포는 사람이 한다.** Claude는 절차와 명령만 쓴다.
  A/S 저장소의 배포 규칙(`RF_Service_System/CLAUDE.md:37`)을 그대로 따른다.
- **마이그레이션 적용·실 데이터 변경·`git push`는 사용자 승인 후에** 한다.
- **PowerShell 스크립트를 새로 만들지 않는다.** NAS는 Linux다. 배치가 필요하면
  셸 스크립트나 TypeScript로 쓴다. (`scripts/`에 옮겨 올 기존 것들은 개발 PC 전용이다.)
- **비밀번호·시크릿을 여기 커밋하지 않는다.** `.env.nas.example`에 자리만 만들고
  실제 값은 NAS에만 둔다.

---

## 알아두면 좋은 것

- **운영은 2026-09-14 부터 NAS 다.** 사내 주소 `http://192.168.0.222:3100·3000·3300`. 개발 PC 서버는 개발용.
- **NAS 접속은 `ssh dss-nas`** — Windows 내장 ssh(`/c/Windows/System32/OpenSSH/ssh.exe`)로 부른다. Git Bash 의 ssh 는
  한글 사용자명 때문에 `~/.ssh/config` 를 못 읽는다. **DSSTECH5 와이파이에서만 닿는다**(dsspublic 은 격리).
  여러 줄 명령은 스크립트 파일을 `'sh -s' < 파일` 로 넘긴다. docker 는 `/usr/local/bin/docker`(비로그인 PATH 에 없음).
- **NAS 의 `.env.nas` · `env/*.env` 는 root 600** 이다. 고칠 때는 `setup/incoming/` 에 올려 두고 사람이 `sudo` 로 옮긴다.
- **env 파일을 훑을 때 값을 절대 출력하지 않는다** — `*_URL` 같은 넓은 패턴이 `DATABASE_URL` 을 끌고 와
  비밀번호가 찍힌 적이 있다(2026-09-14). 키 이름만, 비교는 해시로.
- **`gh`(GitHub CLI)가 이 PC에 없다.** 원격 저장소 생성은 웹에서 하고,
  `git remote add` 이후는 평소대로 쓴다.
- **개발 PC 의 DB 는 2026-09-03 부터 인스턴스 둘이다** — `dss-pg-app` 5442(A/S·계측기 공용) · `dss-pg-auth` 5444.
  홈페이지만 따로 5436. 옛 상자 셋(5432·5434·5438)은 정지, 볼륨은 2026-09-17 이후 회수.
- **PostgreSQL 은 17 로 통일됐다** — 개발 PC · NAS 모두 17.11 · Debian 13 · 한국어 정렬 ICU 76 (2026-09-14 실측).
  16 은 홈페이지 DB(5436) 뿐이다.
- **`njlee`는 저장소 이름과 시스템 이름이 다르다.** 폴더는 `njlee`,
  시스템은 계측기 관리, GitHub은 `dss-meters`다.
- **네 저장소는 서로 다른 GitHub 원격을 가진다.** 한 번에 커밋되지 않으니
  저장소마다 따로 확인한다.
- 공용 실행 스크립트(`start-all.ps1` 등)는 아직 `Development/`에 git 밖으로 떠 있다.
  `scripts/`로 거두는 일은 NAS 이식이 끝난 뒤에 한다 — 이유는 `scripts/README.md`에.
