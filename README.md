# NAS 이식 — 전체 지도

사내 시스템 넷을 개발 PC에서 **Synology DS218+ NAS의 Docker**로 옮긴다.
이 파일이 전체 순서와 **지금 어디까지 왔는지**를 갖는다. 세션을 이어받으면 여기부터 읽는다.

시스템 경로·포트·확정된 결정은 [CLAUDE.md](./CLAUDE.md)에 있다.

---

## 지금 어디까지 됐나

### 🟢 2026-09-14 — 운영 전환 완료. 세 시스템이 NAS 에서 돈다

**16:23~16:31 (중단 약 8분)** 개발 PC 서버 셋을 멈추고 최종 덤프로 NAS 를 다시 세웠다.
**직원이 쓰는 곳은 이제 NAS 뿐**이고, 개발 PC 의 서버·DB 는 개발용이다.

| 시스템 | 사내 주소 |
|---|---|
| 통합 로그인 (앱 목록) | `http://192.168.0.222:3100` |
| A/S 관리 | `http://192.168.0.222:3000` |
| 계측기 관리 | `http://192.168.0.222:3300` |

DSM 리버스 프록시(소스 호스트 `*`)가 위 셋을 `127.0.0.1:13100·13000·13300` 으로 넘긴다.
DSSTECH5 와이파이·사내 유선에서 닿고, **dsspublic 에서는 닿지 않는다**(손님용 격리).

**이날 한 일** — NAS RAM 6GB 인식 → DSM 6.2.2 → 7.1.1 → 수동 **7.2.2 Update 9**(DS218+ 는 7.3 부터
배포 대상이 아니다) → Container Manager 24.0.2 → SSH(열쇠 접속 `dss-nas`) → 구성 수정 · 설정 파일 셋 ·
이미지 1.1 → NAS 준비 `01-prepare.sh` **22/22** → 예행 `02-rehearse.sh` **31/31** → DSM 프록시 셋 →
브라우저 확인 → 전환 `02-rehearse.sh --reset` **33/33** → 야간 백업 `03-install-backup.sh` + DSM 작업
스케줄러 **"DSS 야간 백업" 매일 02:30** (수동·스케줄러 각 1회 성공).

**이날 정한 것** — CLAUDE.md 결정 **F**(주소) · **G**(계측기 원천) · **H**(sudo 는 사람) · **I**(HTTPS 방식).
**이날 사고** — 개발 PC 앱 비밀번호 셋이 화면에 찍혔고 그것이 NAS 비밀번호와 같았다 → NAS 인스턴스를
만들기 전에 `.env.nas` 다섯을 모두 새로 만들었다. 런북과 달랐던 것 열 가지는
[`runbook/01`](./runbook/01-postgres-통합.md) **9절 부록 2**.

> ⚠️ **`02-rehearse.sh --reset` 은 이제 운영 자료를 지운다.** NAS 의 `setup/PRODUCTION` 표지가 있으면
> `--wipe-production` 없이는 거절하도록 막았다. 새 이미지 배포는 아래 「다음 배포」 절차로 한다.

**다음 세션 첫 작업은 아래 「다음 세션 첫 작업」 ① HTTPS (2026-09-15 진행하기로 함).**

---

#### 여기부터는 지난 기록 (2026-09-02 ~ 09-07)

**0단계 완료** — Postgres 통합 계획 확정 (2026-09-02). 결정 A·B 닫힘.

**1단계 진행 중** (2026-09-02)
- ✅ 4번 — `RF_Service_System/HANDOFF.md` 17번을 "staging 대신 배포 리허설"로 갱신
- ✅ 5번 — `dss-home/README.md`에 "이 사이트는 NAS에 올리지 않는다" 명시

**결정 하나 더 닫힘** (2026-09-02) — **회사 홈페이지는 앱도 DB도 NAS에 두지 않는다.**
처음에는 "앱은 NAS, DB만 바깥"으로 갈라져 있었는데 앱까지 바깥으로 정했다.
이제 **NAS는 사내 전용 시스템 셋만** 담는다 — 예외가 없어져 6단계(접근 경계)에서
인터넷에서 들어오는 길을 아예 검토하지 않아도 된다.
근거와 따라가는 결과는 [`runbook/01`](./runbook/01-postgres-통합.md) 4절 결정 A.
- ✅ 3번 — `dss-auth/scripts/backup.ts`에 `BACKUP_MODE` 추가 (`docker` 기본 · `direct` 신규).
  개발 PC 동작은 그대로, NAS용 길이 새로 생겼다. `docker` 모드 실동작 확인함
- ✅ 2번 — 비밀번호 5개 생성, `nas/.env.nas` 작성 완료. 다섯 값이 모두 다르고
  32자이며 URL에 안전한 문자만 쓴 것, git에 노출되지 않는 것까지 확인했다
  (값은 확인 과정에서도 출력하지 않았다)
- ⬜ 1번 — **NAS RAM 6GB 장착.** 남은 하나이자 **4·5단계**의 선행 조건

> **정정 (2026-09-02)** — 앞서 "RAM이 2단계 이후 전부의 선행 조건"이라고 적었는데
> 틀렸다. **2단계는 개발 PC에서 하는 일이라 NAS RAM과 무관하다.**
> RAM이 막는 것은 NAS에 실제로 올리는 4·5단계다.
>
> 다만 2단계는 개발 PC의 `DATABASE_URL`을 세 저장소 모두 바꾼다. **다른 세션이
> 그 저장소에서 작업 중이면 그쪽 개발이 멈춘다** — 특히 `njlee`는 지금
> `sso-integration` 브랜치에서 활발히 진행 중이다. 착수 전에 조율이 필요하다.

**3단계 완료** (2026-09-02) — RAM과 무관하게 먼저 진행했다
- ✅ `RF_Service_System`에 `output: "standalone"` 추가
- ✅ `njlee/.dockerignore` 작성 (계측기는 SMTP 비밀번호를 다뤄 그물이 먼저 필요했다)
- ✅ `Dockerfile` 3개 — 멀티스테이지, `postgresql-client-17`, `node` 사용자로 실행
- ✅ 세 이미지 빌드 · **셋 다 `pg_dump 17.11`**
- ✅ `docker save` → `docker load` 왕복 — **이미지를 지웠다가 되살려** 확인.
  **옮길 파일은 셋 합쳐 324MB**다(`docker images`의 490MB는 압축 풀린 크기)
- ✅ [`nas/docker-compose.nas.yml`](./nas/docker-compose.nas.yml) 초안 —
  문법 검사 통과, **호스트 노출 포트 0개**를 기계적으로 확인
- ⚠️ **4단계로 넘긴 것** — 이미지 안에서 `npm run backup`·`db:migrate`가 안 돈다
  (`tsx`·`drizzle-kit`이 devDependency). 길 셋:
  [`runbook/02-이미지-빌드.md`](./runbook/02-이미지-빌드.md) 8절

**세션 종료 점검에서 잡은 것** (2026-09-02) — `nas/env/*.env`가 **git 추적 대상**이었다.
`.gitignore`의 `.env.*` 패턴은 **점으로 시작하는 이름만** 잡는데 그 파일들은
`auth.env`·`as.env`·`meters.env`라 걸리지 않았다. 거기에는 카카오 시크릿과 SMTP
비밀번호가 들어간다. 파일이 아직 없어 사고가 안 났을 뿐이고, **만드는 순간 커밋된다.**
`nas/env/*.env` 규칙을 추가하고 실제 파일로 시험해 막힌 것을 확인했다.

**6단계 사전 확인** (2026-09-03) — 2단계 리허설을 열었다가 **다른 세션이 아직 돌고 있어
중단했다.** DB도 파일도 건드리지 않았다. 대신 RAM과 무관한 ㄴ으로 갔다.
네 질문 중 셋이 닫혔고, 새 문서 [`runbook/03-원격-접속.md`](./runbook/03-원격-접속.md)에 담았다.

- ✅ 밖에서 쓸 사람 **3~5명**
- ✅ **계측기도 밖에서 쓴다** — 셋 다 VPN으로
- ✅ **공유기가 두 대다** — 바깥 **ipTIME A2004NS-MU**(인터넷 직결), 안쪽 **ASUS RT-AC58U**
- ⬜ 회선 **업로드 속도** — 측정법은 런북 03의 4절 ㄹ
- ⬜ **두 공유기가 어떻게 이어져 있나** (AP 모드인가 · NAS는 어디 물려 있나) — 런북 03의 3절 ㄹ

**여기서 방향이 바뀌었다.** 앞서 *"공유기가 지원하면 NAS 부담 없이 가장 깔끔하다"*고
적었는데, **여기 있는 두 대는 그 전제에 맞지 않는다.**

| | 사정 |
|---|---|
| ipTIME A2004NS-MU (바깥) | ipTIME이 WireGuard를 넣은 펌웨어 **14.20.0의 지원 모델 목록에 없다**(목록은 A3004부터 위). ipTIME은 OpenVPN 서버를 하지 않는다 → 남는 것은 PPTP, 모델에 따라 L2TP |
| ASUS RT-AC58U (안쪽) | WireGuard 없음 · OpenVPN은 710MHz ARM에 무겁다 · **뒤에 있어 포워딩이 두 겹**이 된다 |

**PPTP는 이 저장소의 확정 사항으로 쓰지 않는다.** 그래서 공유기가 다시 후보가 되는
경우는 **ipTIME에 L2TP가 있을 때 하나뿐**이다. 아니면 **NAS가 VPN을 진다** —
DS218+의 Celeron J3355는 AES-NI를 가지고 있어 이 일에 훨씬 적합하고,
공유기는 **포트포워딩 한 줄**만 맡으면 된다. 근거는 런북 03의 3·5절.

**문서 충돌이라 적어 둔 것은 충돌이 아니었다.** `njlee/REQUIREMENTS.md:199`는
**「범위 밖」 목록**에 있고 뜻은 "인터넷에 직접 열지 않는다"다. VPN은 사내망에
들여보내는 것이므로 "사내망 전용"이 그대로 성립한다. 문장을 뒤집을 일이 아니라
한 마디 덧붙일 일이다 — 어디에 무엇을 붙일지는 런북 03의 2절.

**2단계 완료** (2026-09-03) — **개발 PC에서 DB를 인스턴스 둘로 합쳤고, 검증이 전부 통과했다.**
다른 세션이 모두 한가해져 열었다. 런북 01의 9절을 Phase 0~6까지 그대로 밟았고,
앱 중단은 약 7분이었다. 기록과 **런북과 달랐던 것 12가지**는
[`runbook/01`](./runbook/01-postgres-통합.md) **9절 부록**에 있다.

- ✅ [`nas/docker-compose.rehearsal.yml`](./nas/docker-compose.rehearsal.yml) — NAS 파일의 DB 둘을
  `extends`로 끌어오고 포트만 5442·5444로 연다. **NAS 파일은 한 줄도 안 바꿨다**
- ✅ 초기화 스크립트 실동작 — 롤·DB·`CONNECT` 분리. 격리 검증 **소켓·TCP 모두 거절**
- ✅ 덤프 넷 → `pg_restore` **오류 0·경고 0**. 행 수·시퀀스·인덱스·제약·enum 수 전부 일치
- ✅ 세 저장소 `DATABASE_URL` 전환 → `db:preflight` 80/0 · **`check:oidc` 26/26** · 인증 백업 정상
- ✅ 옛 컨테이너 셋 정지(볼륨 보존). **개발 PC는 이제 새 구조(`dss-pg-app`·`dss-pg-auth`)로 돈다**
- ⚠️ **운영에서 고칠 것** — 런북 Phase 1 덤프와 Phase 3 사이에 들어온 자료는 사라진다.
  4단계에서는 **앱을 멈춘 뒤 최종 덤프를 다시 뜬다** (부록 1번)
- ✅ **개발 PC의 시작·종료 스크립트 여섯을 새 구조로 다시 썼다** (2026-09-03, Phase 7에서 앞당김).
  켜기는 저장소의 `db:up` 대신 리허설 compose로 만든 컨테이너를 켜고, **끄기는 상대 서버가
  아직 떠 있으면 끄지 않는다** — A/S와 계측기가 `dss-pg-app` 하나를 나눠 쓰기 때문이다.
  `start-all.ps1`은 컨테이너 둘을 먼저 켜 두 창이 같은 것을 만들려는 경합을 없앤다 (부록 8번)
- ⬜ **Phase 7 — 2026-09-17 이후** 옛 볼륨 넷 회수. 그 전까지는 `docker start` 한 번으로 되돌아간다

**스크립트 왕복 확인** (2026-09-04) — 위 여섯 스크립트를 **처음으로 실제로 돌려 봤다.**
`start-all` → `end-all -Force` 한 바퀴. 새 로직이 두 갈래 다 탔다 — A/S 차례에는
*"계측기(3300)가 아직 쓰고 있어 켜 둡니다"*, 계측기 차례에는 *"정지됨"*.
백업은 `dss_as`에서 2.2MB로 떴고(첨부 76/76 지문 일치), **옛 상자 셋은 한 번도 켜지지 않았으며**
볼륨 여섯은 그대로다. 검증 숫자는 아래 「검증」 절에.

**결정 D — A/S는 업무 자료를 이관하지 않는다** (2026-09-04). 지금 등록된 수리품 258건과
거기 매달린 견적·보고서·고객사·제품모델·재고가 **전부 데모다**(`seed-dev-db.ts`가 만든 것).
**뼈대 20장 · 2,006행만** 가져간다 — 절차·워크플로·작업목록·설정·`users`.
런북 01의 **Phase 1-A**에 명령과 근거가 있고, 개발 PC에서 **덤프→복원까지 실제로 해 봤다**
(20장 전부 일치 · 업무 표 전부 0행). **계측기와 인증은 반대다 — 실제 자료라 통째로 옮긴다.**

> 이 결정은 런북이 원래 가정하던 것을 뒤집는다. Phase 4의 합격 기준도 A/S만 달라진다 —
> "원본과 같다"가 아니라 **"뼈대는 같고 업무 표는 0"**이다. 인수번호가 1번부터
> 시작하는 것도 정상이다.

**「리허설」이 두 가지를 가리킨다는 것을 분명히 해 둔다** (2026-09-04) —
질문을 받고 보니 이 저장소에서 그 말이 두 곳에 쓰이고 있었다.

| | 무엇 | 상태 |
|---|---|---|
| **2단계 「리허설」** | NAS와 **같은 모양**의 DB를 개발 PC에서 먼저 세워 본 것 | ✅ 2026-09-03 |
| **결정 B의 「배포 리허설」** | 상시 staging 대신 택한 것 — **배포 절차 전체를 한 번 예행** | 📄 문서 작성됨 (2026-09-07) · 아직 해 보지 않았다 |

앞의 것만 끝났다. 뒤의 것은 **2026-09-07에 [`runbook/05-배포-리허설.md`](./runbook/05-배포-리허설.md)로 절차가 적혔다.**
다만 **앱은 여전히 컨테이너로 돌아 본 적이 없다** — 이미지는 굽고 `save`/`load` 왕복까지
했지만(3단계) 띄워 본 것은 아니다. 그것이 5단계이고, 05 문서의 **G3(컨테이너 예행)**이
그 첫 시도를 **NAS가 아니라 개발 PC에서** 하게 하는 자리다.

---

## 다음 세션 첫 작업

> **2026-09-14 기준 — 4·5단계가 끝났고 운영은 NAS 다.** 남은 큰일은 6단계의 HTTPS·VPN 과 8단계 정리다.
> 2026-09-07 에 적었던 계획(G3 · 구멍 셋)은 대부분 끝났다 — 구멍 ㄱ·ㄷ 은 메웠고, G3 는 개발 PC 대신
> NAS 예행으로 했다. 남은 것은 아래 ② 표에 옮겼다.

### ① HTTPS — 2026-09-15 진행하기로 함 (사용자)

근거·비용·방법·위험을 담은 발표 자료가 바탕화면 **`HTTPS_도입_제안.pptx`**(13장, 발표자 노트)에 있다. 요지:

- `dss21.com` 의 DNS 는 **카페24** 이고, 인증서 자동 갱신 도구 **acme.sh 가 지원하는 DNS 목록에 카페24·가비아가
  없다**(2026-09-14 확인). 그래서 **사내용 도메인을 새로 사서 DNS 는 Cloudflare(무료)** 에 두고, 인증서는
  **acme.sh 의 DNS 인증(DNS-01)** 으로 받아 `synology_dsm` 배포 훅으로 DSM 에 넣는다. **NAS 를 인터넷에 열지 않는다.**
- 이름 후보 `dss21.net` · `dss21.kr` · `dss21.co.kr` — 09-14 조회 때 DNS 기록 없음(구입 전 확인).
  `.com` 은 2026-11-01 부터 $10.44 → $11.15. `.kr` 은 국내 업체 연 1.2~2.5만 원.
- 지금 HTTPS 가 없어서 **실제로 막힌 것은 A/S 두 가지뿐**: 브라우저 알림 팝업(`isSecureContext`)과 폰 앱 설치(PWA).
  클립보드·UUID 는 이미 http 대체 수단이 있다. 로그인·계측기는 영향 없음. 크롬 154(2026-10)의 http 경고는
  사설 IP 를 "당분간" 뺀다.

**순서 — 첫 명령부터**
1. (사람) 도메인 이름을 정해 구입 → Cloudflare 가입·도메인 연결(네임서버 변경) → **API 토큰**(그 도메인의
   Zone · DNS · Edit 만). ⚠️ **토큰은 채팅에 붙이지 않는다** — `.env.nas` 처럼 파일로 NAS 에 둔다.
2. (사람) Cloudflare 에 A 레코드 셋: `login` · `as` · `meters` → `192.168.0.222` (**프록시 끔 = 회색 구름**).
3. **사내 PC·폰에서 `nslookup as.<도메인>` 이 `192.168.0.222` 를 돌려주는지** 먼저 본다 — 공유기·통신사 DNS 가
   "공개 도메인 → 사설 IP" 응답을 막는지(rebinding 차단). 막히면 여기서 멈추고 공유기 DNS 설정을 본다.
4. (Claude) acme.sh 를 NAS 에서 돌리는 스크립트 `nas/setup/04-*` — 컨테이너(`neilpang/acme.sh`)로 와일드카드
   발급 + `synology_dsm` 훅. 갱신은 DSM 작업 스케줄러. 참고: acme.sh wiki "Synology NAS Guide".
5. (사람) DSM 리버스 프록시를 **HTTPS 443 · 호스트 이름 기준** 규칙 셋으로.
6. (저녁 10분) 주소 전환 — 시스템마다 네 곳:
   env 파일(`OIDC_ISSUER`/`SSO_ISSUER` · `SSO_REDIRECT_URI` · `KAKAO_REDIRECT_URI` · 계측기 `SITE_URL`) +
   포털 등록(NAS DB 의 `clients` — `02-rehearse.sh` 5단계와 같은 SQL 방식) + 카카오 콘솔.
   함께 `OIDC_ALLOW_HTTP_REDIRECT_URIS=false`, 계측기 `SESSION_COOKIE_SECURE=true`.
   env 파일은 NAS 에서 root 600 이라 **`sudo` 로 바꾸고 `docker compose up -d` 로 앱만 다시 띄운다(DB 는 그대로).**
7. 되돌리기 — 지금 http 설정(env 셋 · 프록시 규칙 셋)을 그대로 보관해 둔다.

### ② 전환 뒤 남은 것 — 급한 순서

| | 할 일 | 기한 · 이유 |
|---|---|---|
| 1 | **이남준 님 PC** — 계측기 앱 끄기, 예약 작업 둘(`계측기 관리 시스템 백업` 18:30 · 교정 알림 09:00) "사용 안 함" | 사용자에게 부탁함, **완료 여부 미확인**. 그대로면 옛 계측기가 따로 돌고 10/1 알림 메일이 두 번 간다 |
| 2 | **교정 알림 메일(`send-notify`)을 NAS 에서** — DSM 작업 스케줄러로 매일 09:00 (실제 발송은 매월 1일) | **2026-10-01 전.** `tsx` 가 운영 이미지에 없다 → 도구 이미지(runbook/05 6절 길 ㄴ)가 필요하다. 메일 계정은 `meters.env` 에 있다 |
| 3 | NAS `setup/dumps/`(전환 최종 덤프 7개, 실자료)를 `backups/cutover-2026-09-14/`(root)로, `images/`(325M) 정리 | 사람이 `sudo`. 지금은 administrators 만 읽는 곳 |
| 4 | A/S `users` 의 데모 계정 정리 | 뼈대에 딸려 온 계정(런북 01 Phase 1-A) |
| 5 | **Phase 7 — 2026-09-17 이후**: 개발 PC 옛 볼륨 넷 회수 · `rehearsal/`(1.9MB 실자료) · `dss-auth/docker-compose.yml` · `DEV_POSTGRES_PASSWORD` | 아래 표의 계획 그대로. 그 전에는 지우지 않는다 |
| 6 | NAS 밖 백업 사본 (Hyper Backup → USB·클라우드) | 지금 백업은 같은 디스크 안 |
| 7 | 도구 이미지 · `db:preflight` 「되돌리기 불가」 목록(05 11-ㄴ, A/S 저장소) | 다음 A/S 마이그레이션 전에 |
| 8 | VPN (6단계) — 런북 03 의 남은 확인 넷 | 밖에서 쓸 사람이 생길 때 |
| 9 | 개발 PC 의 A/S·계측기·포털이 **여전히 `{lan}` 으로 사내망에 보인다** — 직원이 옛 주소로 들어오면 개발 DB 에 쓴다 | 공지로 막았다. 필요하면 개발 서버를 `localhost` 전용으로 |

### 다음 배포(새 이미지) — 이 날 만든 길

1. (개발 PC) `git archive` 로 **커밋된 상태만** 뽑아 `docker build` → `docker save` → NAS `images/` 로 보내고 지문 대조.
   (2026-09-14 에는 세션 임시 폴더의 스크립트로 했다 — `scripts/` 로 거둘 것)
2. (NAS · 사람 · `sudo`) `docker load` → compose 의 이미지 태그를 올리고 `docker compose ... up -d app-…` —
   **DB 는 건드리지 않는다.** 마이그레이션이 있으면 runbook/05 G4 · G6.
3. ⚠️ `02-rehearse.sh --reset` 은 배포가 아니다 — **DB 를 지우고 전환 때 덤프로 되돌리는 것**이다.

### 검증 — 2026-09-14 실제로 돌린 값

| 무엇 | 어디서 | 값 |
|---|---|---|
| NAS 준비 `01-prepare.sh` | NAS | **22/22** — ACL 걷기·권한, 이미지 셋(git `b1436ba` · `051a949` · `a7f71d1`), 실제 컨테이너 읽기·쓰기 시험 6 |
| 예행 `02-rehearse.sh` | NAS | **31/31 · 46초** (DB 정상까지 26초) |
| 전환 `02-rehearse.sh --reset` | NAS | **33/33 · 58초** — 예행 때 첨부 5개 정리, 계측기 파일 463 |
| 격리 검증 | NAS 두 인스턴스 | 계측기→A/S · A/S→계측기 **소켓·TCP 모두 거절**, 업무 롤은 인증 인스턴스에 없음, 제 DB 접속 3건 정상 |
| 행 수 대조 | NAS | **표 111개 전부 일치** — `dss_auth` 9표 1,423행 · `dss_meters` 11표 1,105행(`web_meters` 79) · `dss_as` 91표 2,111행(뼈대 20표 2,013 + 마이그레이션 98, `repair_cases` **0**) |
| 한글 정렬 | NAS | `123 < 가나 < 다람쥐 < apple < Zebra` (ICU ko-KR) |
| 포털 `iss` | 이 PC → 프록시 | `http://192.168.0.222:3100` (컨테이너 주소 아님) |
| 로그인 흐름 | 이 PC → 프록시 | A/S · 계측기 모두 포털 `/signin` 200 까지 · 안쪽 포트 13000·13100·13300 은 사내망에서 막힘 |
| 브라우저 (사용자) | 예행 | 로그인 · 계측기 79대·사진·성적서 · A/S 접수(D2609…)·알림 메일·첨부 · 견적서/보고서 출력 · 로그아웃 통보 **전부 됨**. **전환 뒤 최종 로그인 확인은 보고받지 못했다** |
| 야간 백업 | NAS | 수동 1회 · 작업 스케줄러 1회 **성공** — auth 132K · meters 100K · as 520K · 파일 463 · 2~3초 |
| 자원 | NAS | 컨테이너 다섯 약 350MB · NAS 사용 약 850MB / 5,786MB · `/volume1` 여유 2.1T |
| A/S 마이그레이션 | 개발 PC | 코드 98 = DB 98 (전환 직전). 세션 끝에는 Docker Desktop 이 꺼져 있어 `db:preflight` 가 접속하지 못했다 |

**돌리지 않은 것** — `npm test`(dss-auth·A/S) · `npm run test:db` · `npm run check:oidc` 는 돌리지 않았다.
앱 코드를 고치지 않았고, `check:oidc` 는 NAS 컨테이너에 `tsx` 가 없어 거기서 돌릴 수 없다.
세션 끝에 개발 PC 의 컨테이너·볼륨은 **Docker Desktop 이 꺼져 있어 확인하지 못했다** — 세션 시작 때 여섯이
모두 있었고, 이 세션에서 지운 것은 없다.
### 검증 — 2026-09-04 실제로 돌린 값

| 명령 | 어디서 | 값 |
|---|---|---|
| `npm run db:preflight` | A/S | **전체 82건 · 적용 대기 0건** (세션 중 80→82로 늘었다 — 다른 세션이 적용) |
| `npm run check:oidc` | dss-auth | **통과 26 / 실패 0** (`{lan}`이 `192.168.1.132`로 펼쳐짐) |
| 격리 검증 | `dss-pg-app` | 계측기→`dss_as` · A/S→`dss_meters` **둘 다 `permission denied`** (정상) |
| 격리 검증 | `dss-pg-auth` | 업무 롤 `dss_app`이 **아예 존재하지 않음** (인스턴스가 갈려 있다는 뜻) |
| `start-all` → `end-all -Force` | 개발 PC | 왕복 통과. 백업 `dss_as_2026-09-04_0916.sql` 2.2MB · 첨부 76/76 |
| **뼈대 덤프→복원** (결정 D) | 개발 PC | 임시 DB에 스키마 넣고 복원. `TABLE DATA` **정확히 20개** · **20장 전부 행 수 일치(2,006)** · 업무 표 전부 **0행** · `drizzle` journal 0개. 임시 DB는 삭제함 |

**기준선 (결정 D를 실행할 때 비교할 값)** — `repair_cases` **258** · `web_meters` **79**.
A/S는 이관 후 `repair_cases`가 **0이어야** 정상이고, 계측기는 79 그대로여야 한다.

**돌리지 않은 것** — `npm test`(dss-auth · A/S)와 `npm run test:db`는 이번 세션에서 돌리지 않았다.
이쪽 변경이 문서와 PowerShell 스크립트뿐이라 앱 코드를 건드리지 않았다.
**A/S는 지금 다른 세션이 앱 코드를 고치는 중이라 그쪽 테스트를 건드리지 않는 편이 낫다** (아래 인계 메모).

### ㄱ. ~~2단계 리허설~~ — **완료** (2026-09-03)

개발 PC에서 DB를 인스턴스 둘로 합쳐 형태를 검증했다. 결과와 배운 것은 런북 01의 9절 부록.

### ㄴ. ~~6단계 사전 확인~~ — **보류** (2026-09-03)

> ⏸ **여기까지만 하고 접었다.** VPN은 6단계이고 그 앞에 4·5단계가 있다.
> 남은 확인(아래 표)은 **6단계를 실제로 열 때** 한다 — 지금 재촉할 이유가 없다.
> 알아낸 것은 [`runbook/03-원격-접속.md`](./runbook/03-원격-접속.md)에 다 들어 있다.
> **다음 세션은 이 항목을 다음 작업으로 고르지 않는다.**

<details>
<summary>나중에 6단계를 열 때 볼 것 (펼치기)</summary>

남은 것은 **회선 업로드 속도**와 **공유기 관리 페이지 확인** 둘뿐이다.
둘 다 사람이 직접 보는 일이고, 절차는 [`runbook/03-원격-접속.md`](./runbook/03-원격-접속.md) 4절에 있다.

| | 볼 것 | 어디서 |
|---|---|---|
| 1 | **ipTIME에 `L2TP`가 있는가** (PPTP만인가) | `192.168.0.1` → 고급 설정 → 특수 기능 → VPN 서버 설정 |
| 2 | 외부 IP가 **공인 IP인가** | ipTIME의 외부 IP ↔ `ifconfig.me` 대조 |
| 3 | ASUS가 **AP 모드인가**, **NAS는 어디에** 물려 있나 | ASUS 관리 페이지 동작 모드 · 랜선 확인 |
| 4 | **업로드** 속도 | 사내 유선 PC에서 `fast.com` → 자세히 보기 |

**1번이 가장 크다.** `L2TP`가 있으면 공유기 길이 다시 열리고, `PPTP`만 있으면 닫힌다.
이 넷이 나오면 VPN 방식이 바로 정해진다(런북 03 5절). **코드 작업이 아니다.**

</details>

---

## 인계 메모

- **2026-09-14 저녁 상태** — NAS: DB 둘 · 앱 셋 운영 중(로그 `/volume1/dss/setup/logs/`). 개발 PC: 서버 넷 모두
  꺼짐, **Docker Desktop 꺼짐**(컨테이너·볼륨 미확인 — 지운 것은 없다). 다섯 저장소 중 미커밋은 dss-deploy 뿐.
- **NAS 접속** — `ssh dss-nas`(Windows 내장 ssh, 열쇠 `~/.ssh/dss_nas_ed25519`, 계정 `swhur`). **DSSTECH5 에서만 닿는다.**
  docker 는 `sudo` 가 필요해 사람이 SSH 창에서 `sudo -i` 뒤 스크립트를 돌리고, Claude 는 로그를 읽는다(결정 H).
  비밀번호를 치게 할 때는 **한/영부터 확인**시킨다 — 한글 입력으로 틀린 적이 있다.
- **NAS 의 `/volume1/dss/`** — `deploy/`(compose · `.env.nas` · `env/` · `init/`, root) · `as-attachments/` · `as-templates/` ·
  `meters-files/` · `auth-keys/`(node 1000) · `backups/`(root) · `jobs/`(root) · `setup/`(스크립트·로그·덤프, administrators).
- **개발 PC 의 `nas/.env.nas` 와 `nas/env/*.env` 는 지우지 않았다** — 다음 배포 때 원본이 필요하다. 원래 계획
  ("NAS 로 가져간 뒤 개발 PC 사본을 지운다")을 바꾼 것이다. 둘 다 git 제외 확인. 리허설 compose 가 `.env.nas` 를
  읽으므로 개발 인스턴스를 **처음부터 다시 만들면** 개발과 운영 비밀번호가 다시 같아진다 — 그때는 따로 만들 것.
- ~~A/S에 적용 대기 마이그레이션 1건 (`0078_blue_bullseye`)~~ — **해소됐다.**
  2026-09-04 기준 `npm run db:preflight`가 *"전체 82건 · 적용 대기 0건"*이라고 답한다.
- ✅ (2026-09-14 해소 — 그쪽 작업이 커밋·푸시돼 저장소가 깨끗하다) ~~`RF_Service_System`에서 다른 세션이 작업 중이다 (2026-09-04 17시경 확인).~~
  내 커밋(`8aef3b1`) 뒤로 커밋 셋이 더 쌓였고(`d3203af`·`041abfa`·`318f8d1` — 데모판 걷어내기,
  검수/승인 화면, 작업 비용 메뉴), **푸시 안 한 커밋 1개**와 **미커밋 3개**
  (`package.json`, `scripts/load-template-env.ts`, `scripts/test-template-bootstrap.ts`)가 있다.
  **그쪽 것이므로 커밋하지도 되돌리지도 않았다.** 이식 쪽이 고친 `start-work.ps1`·`end-work.ps1`은
  그대로 남아 있는 것을 확인했다. 그 저장소를 만질 일이 생기면 **먼저 조율한다.**
- ~~`.env.nas`는 NAS로 가져간 뒤 개발 PC 쪽 사본을 지운다~~ — **바꿨다 (2026-09-14)**. 위 첫 항목 참고.
- `njlee`는 브랜치가 `main`이 아니라 **`sso-integration`**이다.
  그 저장소에서는 `git add -A`를 쓰지 말고 파일을 지정한다.
- ~~**`RF_Service_System`에 다른 세션의 미커밋 작업이 있다 (2026-09-03 확인).**~~ — **해소됐다.**
  그쪽 세션이 커밋했고, 그 뒤 2단계 리허설을 열었다.
- **개발 PC의 DB 구조가 바뀌었다 (2026-09-03).** 세 앱은 이제 `dss-pg-app`(5442, `dss_as`·`dss_as_test`·`dss_meters`)과
  `dss-pg-auth`(5444, `dss_auth`)를 본다. 옛 개발 컨테이너 셋은 **정지만 했고** 볼륨은 그대로다.
  `rehearsal/`(git 제외)에 덤프 넷·행 수 스냅숏·복원 로그·**`.env.local` 원본 사본**이 있다.
  ~~시작 스크립트가 옛 상자를 다시 켜더라도 앱은 새 인스턴스를 보므로 무해하다.~~
  **더는 켜지 않는다 (2026-09-04)** — 시작·종료 스크립트 여섯이 새 인스턴스만 본다.
- **A/S staging 컨테이너는 평소에 안 떠 있다.** 없어진 것이 아니라
  `docker-compose.yml:40`의 **profile**로 묶여 있어 `--profile staging`일 때만 만들어진다.
  2단계에서 "5433이 안 보인다"고 당황하지 않도록 적어 둔다.
- **컨테이너·볼륨 (2026-09-04 저녁, 마지막 확인)** — 떠 있는 것 셋:
  `dss-pg-app`(5442) · `dss-pg-auth`(5444) · `dss-home-postgres-dev`(5436).
  `dss-pg-app`이 담은 DB는 `dss_as`·`dss_as_test`·`dss_meters` 셋이다 —
  결정 D 검증에 쓴 임시 DB(`skel_test`)는 **지웠고 남아 있지 않다.**
  옛 개발 DB 셋(`dss-as`·`dss-auth`·`dss-meters`-postgres-dev)은 **정지**돼 있고,
  이제 시작 스크립트가 그것들을 켜지 않는다.
  볼륨은 옛 넷 + 새 둘 = **여섯이 모두 살아 있다. 2026-09-17 전에는 지우지 않는다.**
  세션을 닫는 시점에 **사용자가 「작업 시작」으로 네 시스템을 띄워 둔 상태**였다
  (3000·3100·3200·3300 전부 떠 있음). 끄지 않았다 — 세션을 닫는 것과 서버를 끄는 것은 별개다.

**RAM 6GB는 4·5단계의 선행 조건이다** — 2단계는 개발 PC에서 하는 일이라 무관하다
(위 2026-09-02 정정). 이 줄이 앞과 어긋나 있어 바로잡았다 (2026-09-03).

---

## 단계

| | 단계 | 무엇을 하나 | 상태 |
|---|---|---|---|
| 0 | 계획 | Postgres 인스턴스를 둘로 줄이는 계획. 결정 A·B 확정 | ✅ |
| 1 | 준비 | 비밀번호 5개(09-14 재생성) · `dss-auth` 백업 모드 · RAM 6GB · DSM 7.2.2 · Container Manager · SSH | ✅ |
| 2 | **DB** 리허설 | **개발 PC에서 먼저** DB를 둘로 통합해 형태를 검증 | ✅ |
| 3 | 이미지 | `Dockerfile` 3개, A/S에 `output: "standalone"`, 이미지에 `postgresql-client-17` | ✅ |
| 4 | DB 이전 | NAS에 인스턴스 둘. 계측기·인증은 **통째로**(계측기는 개발 PC 사본, 결정 G), A/S는 **뼈대 20장만** (결정 D) | ✅ 09-14 |
| 5 | 앱 이전 | NAS에 앱 컨테이너 셋(이미지 1.1), 앱 포트는 `127.0.0.1:13xxx` 에만 | ✅ 09-14 |
| 6 | 접근 경계 | ~~리버스 프록시~~ ✅ · ~~`TRUSTED_PROXY_HOPS=1`~~ ✅ · 방화벽(DSM 꺼짐 · 앱 포트는 127.0.0.1) · **HTTPS ⬜ 09-15 착수** · **VPN ⬜** | 🔶 |
| 7 | 주소 갱신 | NAS 사본의 포털 등록에 `192.168.0.222` 주소 · 카카오 콘솔 · 로그아웃 통보 확인 (HTTPS 때 한 번 더) | ✅ 09-14 |
| 8 | 정리 | ~~야간 백업 02:30~~ ✅ · 옛 볼륨 회수(09-17 이후) ⬜ · NAS 밖 사본 ⬜ · 교정 알림 예약 작업(10/1 전) ⬜ | 🔶 |

> ⚠️ **2단계의 「리허설」과 결정 B의 「배포 리허설」은 다른 것이다.** 2단계는 *DB의 모양*을
> 개발 PC에서 먼저 만들어 본 것이고(끝났다), 결정 B가 약속한 것은 *배포 절차 전체*를
> 한 번 예행해 보는 것이다(`runbook/05-배포-리허설.md` — 2026-09-07 작성, 2026-09-14 NAS 예행으로 실제로 해 봤다).

### 왜 2단계(DB 리허설)를 먼저 두는가

개발 PC와 NAS의 **형태가 다르면 NAS에서만 나오는 버그가 생긴다.** 개발 PC에서 먼저
같은 구조(인스턴스 둘, PostgreSQL 17, 롤·`CONNECT` 분리)로 만들어 보면, 4단계에서
NAS는 이미 검증된 형태를 그대로 세우기만 하면 된다.

같은 이유로 결정 B에서 상시 staging 대신 **배포 리허설**을 택했다. 처음 해 보는 일을
운영 장비에서 처음 하지 않는다.

---

## 문서

| 문서 | 내용 |
|---|---|
| [CLAUDE.md](./CLAUDE.md) | 저장소 넷의 경로·포트, 확정된 결정, 지켜야 할 것 |
| [runbook/01-postgres-통합.md](./runbook/01-postgres-통합.md) | 0·2·4단계 — 인스턴스 둘로 줄이기, 권한 설계, Phase 0–7 절차 |
| [runbook/02-이미지-빌드.md](./runbook/02-이미지-빌드.md) | 3단계 — 빌드와 실행의 차이, 이미지를 NAS로 옮기는 법 |
| [runbook/03-원격-접속.md](./runbook/03-원격-접속.md) | 6단계 앞쪽 — **밖에서 사내망으로 들어오는 길(VPN)**. 공유기 사정과 갈림길 |
| [runbook/05-배포-리허설.md](./runbook/05-배포-리허설.md) | **결정 B가 약속한 것** — 새 기능이 운영에 닿기까지의 관문 일곱, 저녁 정지 창, 되돌리기, 확장-수축. 런북 02 8절을 길 ㄴ으로 닫는다 |
| [nas/init/app/01-roles.sh](./nas/init/app/01-roles.sh) | 업무용 인스턴스의 롤·DB·권한 |
| [nas/init/auth/01-roles.sh](./nas/init/auth/01-roles.sh) | 인증용 인스턴스의 롤·DB·권한 |
| [nas/docker-compose.nas.yml](./nas/docker-compose.nas.yml) | **운영 구성** — 2026-09-14 부터 NAS 에서 돈다. DB 둘·앱 셋, DB 포트 없음, 앱 포트는 `127.0.0.1` 에만 |
| [nas/docker-compose.rehearsal.yml](./nas/docker-compose.rehearsal.yml) | **개발 PC 리허설용** — NAS 파일의 DB 둘을 `extends`로 끌어오고 포트만 연다. NAS로 가져가지 않는다 |
| [nas/.env.nas.example](./nas/.env.nas.example) | 두 인스턴스가 읽는 비밀번호 자리 |
| [nas/env/README.md](./nas/env/README.md) | 앱마다의 설정 파일을 만드는 법과 NAS에서 달라지는 값 |
| [scripts/README.md](./scripts/README.md) | 공용 실행 스크립트를 아직 옮기지 않은 이유 |
| [nas/setup/01-prepare.sh](./nas/setup/01-prepare.sh) | NAS 준비 — `dss` 폴더 권한(ACL → node 1000 · postgres 999), 이미지 불러오기, 컨테이너 시험. 사람이 `sudo` |
| [nas/setup/02-rehearse.sh](./nas/setup/02-rehearse.sh) | DB 둘 → 격리 검증 → 복원 → 행 수 대조 → 포털 등록(NAS 사본) → 앱 셋. **`--reset` 은 운영 자료를 지운다** |
| [nas/setup/03-install-backup.sh](./nas/setup/03-install-backup.sh) | 야간 백업 스크립트를 root 만 고칠 수 있게 제자리에 두고 한 번 돌린다 |
| [nas/jobs/backup-nightly.sh](./nas/jobs/backup-nightly.sh) | 매일 02:30 (DSM 작업 스케줄러, root) — DB 셋 · 파일. 빈 백업은 남기지 않고, 개수로 정리 |

앞으로 늘어날 것: `04-리버스-프록시.md`(사내망 안에서 앱에 닿는 길)

---

## 1단계 — ~~지금 할 일~~ 끝났다 (2026-09-14)

| | 할 일 | 어디서 |
|---|---|---|
| 1 | NAS RAM 6GB 장착, DSM 정보 센터에서 인식 확인 | NAS |
| 2 | 비밀번호 **5개** 생성 (**모두 서로 다른 값**) | 아무 데서나 |
| 3 | `dss-auth/scripts/backup.ts`를 TCP 접속 방식으로 수정 | `dss-auth` |
| 4 | ~~`RF_Service_System/HANDOFF.md` 17번 갱신~~ ✅ 완료 | `RF_Service_System` |
| 5 | ~~`dss-home/README.md`에 "NAS에 올리지 않는다" 명시~~ ✅ 완료 | `dss-home` |

### 3번 — 처음 파악이 반대였다 (2026-09-02 정정)

계획서 초안은 "`njlee`의 백업이 NAS에서 안 돈다"고 적었지만, 실제로 읽어 보니
**반대였다.**

| | 지금 방식 | NAS에서 |
|---|---|---|
| `njlee/scripts/backup.ts` | `DATABASE_URL`로 TCP 접속 · 비밀번호는 `PGPASSWORD` | **이미 준비돼 있다** |
| `dss-auth/scripts/backup.ts` | `docker exec <컨테이너> pg_dump` | **돌지 않는다** |

`njlee`는 128행에 *"PG_BIN이 비어 있으면 PATH에서 찾는다 (NAS 컨테이너에서는
그쪽이 맞다)"*고 적혀 있다. 처음부터 컨테이너를 염두에 두고 쓴 코드다.
남은 것은 코드가 아니라 **이미지에 `postgresql-client-17`을 넣는 일**(3단계)뿐이다.

문제는 `dss-auth`다. `docker exec`를 부르므로 컨테이너 안에서는 돌지 않는다.
**돌게 만들려면 Docker 소켓을 앱 컨테이너에 물려야 하는데, 소켓 접근은 사실상
호스트 root 권한이다.** 하필 인증 컨테이너에 그걸 주는 것은 이 프로젝트가
인증 DB를 따로 격리한 이유를 정면으로 무너뜨린다.

→ **`dss-auth`를 `njlee` 방식(TCP + `PGPASSWORD`)으로 바꾼다.** 개발 PC에서
`docker exec`를 쓰던 이점(호스트에 클라이언트가 없어도 됨)은 모드 선택으로 남긴다.

비밀번호는 **셋이 아니라 다섯**이다. 앱 롤 셋(`AS_APP_PASSWORD`·`METERS_APP_PASSWORD`·
`AUTH_APP_PASSWORD`)에 더해, 상자를 세울 때 쓰는 부트스트랩 관리자 둘
(`APP_POSTGRES_PASSWORD`·`AUTH_POSTGRES_PASSWORD`)이 필요하다.
빈칸은 [`nas/.env.nas.example`](./nas/.env.nas.example)에 전부 있다.

아래 명령을 다섯 번 실행한다.

```
node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
```

`base64url`인 이유: 이 값이 `DATABASE_URL` 안에 들어가는데, `@`·`:`·`/`가 섞이면
주소가 엉뚱하게 쪼개진다.

⚠️ **비밀번호는 상자를 처음 만들 때 굳는다.** 나중에 `.env.nas`만 고쳐도 이미
만들어진 롤은 바뀌지 않는다(초기화 스크립트가 빈 데이터 폴더에서만 돈다).
그래서 이 항목이 4단계보다 앞에 있다.

---

## 6단계에서 정할 것 — VPN 원격 접속 (2026-09-02 방향 확정)

**회사 밖에서 VPN을 통해 사내 시스템을 쓰기로 한다.** 인터넷에 앱을 직접 열지 않는다.

이 방향이 정해져 있으면 6단계 설계가 단순해진다 — VPN 사용자도 **사내 주소를 받으므로**
포털에 등록할 주소가 늘지 않고, 앱 설정을 하나도 바꾸지 않아도 된다.
`RF_Service_System/SECURITY_POLICY.md`에 원래 그렇게 적혀 있던 것이기도 하다.

**자세한 것은 [`runbook/03-원격-접속.md`](./runbook/03-원격-접속.md)로 옮겼다.** 여기에는 결과만 둔다.

착수 전 알아 둘 것 넷 중 **셋이 닫혔다** (2026-09-03).

| | 확인할 것 | 답 |
|---|---|---|
| 1 | 공유기가 WireGuard·OpenVPN을 지원하는가 | **둘 다 아니다.** 바깥 **ipTIME A2004NS-MU**는 WireGuard 지원 목록 밖 · OpenVPN 미지원, 안쪽 **ASUS RT-AC58U**는 WireGuard 없음 → 아래 |
| 2 | 회사 인터넷의 **업로드** 속도 | ⬜ **아직** — 이것이 남은 갈림길이다 |
| 3 | 밖에서 쓸 사람이 몇 명인가 | **3~5명** |
| 4 | **계측기도 밖에서 쓸 것인가** | **예 — 셋 다 VPN으로** |

> **앞서 적은 방향이 바뀌었다.** *"공유기가 지원하면 NAS 부담 없이 가장 깔끔하다"*는
> **공유기가 그 일을 감당할 때** 맞는 말이다. 여기 두 대는 그렇지 않다.
> 남는 프로토콜이 **PPTP**뿐이면 공유기 길은 닫힌다(⛔ 아래). 공유기가 다시
> 후보가 되는 경우는 **ipTIME에 L2TP가 있을 때 하나**다. 아니면 **NAS가 진다** —
> 공유기는 포트포워딩만 맡는다. 런북 03의 3·5절.

> ✅ **문서 충돌은 충돌이 아니었다.** `njlee/REQUIREMENTS.md:199`는 **「범위 밖」 목록**에
> 있고 뜻은 "인터넷에 직접 열지 않는다"다. VPN은 사내망에 들여보내는 것이므로
> "사내망 전용"이 그대로 성립한다. 정리 문장은 그대로 쓴다 —
> **"VPN으로 들어온 사람도 사내에 있는 것으로 본다."**
> 두 문서에 한 마디씩 덧붙일 자리는 런북 03의 2절에 적어 두었다.

⛔ PPTP는 쓰지 않는다. 설정이 가장 쉽지만 암호가 이미 깨진 방식이다.

---

## 나중에 반드시 확인할 것

> **2026-09-14 — 아래 표의 「사내망 IP 로 계속 운영」을 골랐다(결정 F).** 세 env 파일에 `http://192.168.0.222:…` 를
> 직접 적었고(`auto` 안 씀), NAS 사본의 포털 등록에 같은 주소를 더했다. 포털 `iss` 가 컨테이너 주소가 아닌 것을
> 확인했다. **HTTPS 때 「리버스 프록시 뒤 도메인」으로 넘어간다**(결정 I) — 그때 네 곳을 한 번 더 바꾼다.

**주소가 바뀌면 고칠 곳이 시스템마다 네 군데였다.** 지금은 개발 PC 안에서는
0군데다 — `.env.local` 은 `auto`, 포털 등록값은 `{lan}` 자리표시자를 쓰고,
둘 다 실행 시점에 그 기계의 실제 사내망 IPv4 를 찾는다.
판정 규칙과 근거는 `dss-auth/docs/주소.md`, 현재 상태는 포털에서
`npm run net:doctor`.

포털은 여전히 이 주소를 **글자 단위로 정확히** 대조한다. `{lan}` 은 비교
직전에 포털 자신의 주소로 펼쳐질 뿐이고, 와일드카드도 정규화도 늘지 않았다
(`dss-auth/src/lib/db/schema/clients.ts:32` 과
`dss-auth/src/lib/oidc/redirect-uri.ts` 에 그 이유가 있다).

### ⚠️ 7단계에서 물릴 자리 — 컨테이너 안에서는 `auto` 가 통하지 않는다

`auto` 와 `{lan}` 은 `os.networkInterfaces()` 를 읽는다. **브리지 네트워크로
띄운 컨테이너 안에서는 그것이 NAS 의 사내망 주소가 아니라 컨테이너의 주소
(172.17.x.x 같은 값)** 다. 그대로 두면 발급되는 ID 토큰의 `iss` 가 아무도
닿을 수 없는 주소가 되고, redirect_uri 는 어느 것과도 일치하지 않는다.

증상은 여느 때와 같이 "로그인이 안 된다" 하나뿐이므로, NAS 로 옮길 때는
**옮기기 전에** 아래 중 하나를 정해 두어야 한다.

| 방식 | 설정 |
|---|---|
| 리버스 프록시 뒤 도메인 (권장) | `OIDC_ISSUER=https://<도메인>` 직접 적고, 등록값도 그 도메인으로. `OIDC_ALLOW_HTTP_REDIRECT_URIS=false` 로 되돌린다 |
| 사내망 IP 로 계속 운영 | NAS 의 고정 IP 를 직접 적는다 (`auto` 를 쓰지 않는다) |
| host 네트워크로 띄움 | `auto` 가 그대로 동작하지만, 포트 충돌을 직접 관리해야 한다 |

어느 쪽이든 7단계의 "주소 갱신" 항목에서 `npm run net:doctor` 로 확인한 값이
실제 접속 주소와 같은지 눈으로 대조하고 넘어간다.
