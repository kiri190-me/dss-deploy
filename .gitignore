# 실제 값이 든 환경 파일. 예시(.env.nas.example)만 추적한다.
.env
.env.*
!.env.*.example

# DB 덤프. 실 데이터가 들어 있으므로 절대 커밋하지 않는다.
*.dump
*.sql.gz
before-*.txt
after-*.txt

# 이관 중 만들어지는 작업 파일
rehearsal/
tmp/

# OS
Thumbs.db
.DS_Store
